<% header = link_to('All Posts', posts_path) + "&raquo; #{@category.name}".html_safe %>

<%= render 'shared/title', title: header %>


<%= render 'shared/post_display', post_object: @category.posts %>